language: R
sudo: false
cache: packages
dist: xenial

apt_packages:
  - libgetdns-dev
  - libidn2-0-dev
  - libunbound-dev

addons:
  apt:
    update: true

after_success:
  - Rscript -e 'covr::codecov()'
